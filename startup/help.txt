go install github.com/air-verse/air@latest

go mod download
go mod tidy

go install .



go build -o prebid-server.bin .

pm2 start ecosystem.config.js

BUILD_PREBID=true pm2 restart startup/ecosystem.config.js


rm -f /opt/service_logs/ssp_module/ssp_bids/*log
BUILD_PREBID=true pm2 restart ecosystem.config.js

pm2 list

sudo truncate -s 0 /opt/service_logs/ssp_module/ssp_bids/*.log 
=========

npm install pm2@latest -g

pm2 startup
pm2 -v
pm2 list

sudo apt update
sudo apt install -y certbot

sudo apt install -y python3-certbot-nginx

sudo ln -s /etc/ngnix/sites-available/nginx_instance_1.conf /etc/nginx/sites-enabled/

sudo certbot --nginx 

===============

SG_ID=$(aws ec2 describe-instances --query 'Reservations[*].Instances[*].SecurityGroups[0].GroupId' --output text)
# Open Port 3000 (Grafana)
aws ec2 authorize-security-group-ingress --group-id $SG_ID --protocol tcp --port 3000 --cidr 0.0.0.0/0
# Open Port 3306 (MySQL)
aws ec2 authorize-security-group-ingress --group-id $SG_ID --protocol tcp --port 3306 --cidr 0.0.0.0/0




# Allow Grafana
sudo ufw allow 3000/tcp
# Allow MySQL
sudo ufw allow 3306/tcp
# Reload firewall
sudo ufw reload


=====================


sudo cp /opt/adserving/14-feb-2026/1_prebid-server/startup/prometheus/ssp_rules.yml /etc/prometheus/rules/
sudo cp /opt/adserving/14-feb-2026/1_prebid-server/startup/prometheus/dsp_rules.yml /etc/prometheus/rules/

# Backup existing config
sudo cp /etc/prometheus/prometheus.yml /etc/prometheus/prometheus.yml.bak
# Replace the file with your optimized workspace version
sudo cp /opt/adserving/14-feb-2026/1_prebid-server/startup/prometheus/prometheus.yml /etc/prometheus/prometheus.yml
curl -X POST http://localhost:9090/-/reload



sudo systemctl restart prometheus
# Or use the reload API if enabled:
# curl -X POST http://localhost:9090/-/reload


============================